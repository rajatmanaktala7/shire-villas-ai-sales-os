# Shire Villas AI Sales OS - One-Time Railway Fix

Upload these files directly to the ROOT of the existing GitHub repository.
Do not upload this folder itself and do not upload the ZIP itself.

Required Railway variable:
DATABASE_URL=${{Postgres.DATABASE_URL}}

After successful deploy test:
/health
/
/docs

Expected health response includes version 2.0.0.
